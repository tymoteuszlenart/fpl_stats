[tool.ruff]
line-length = 100
target-version = "py310"
src = ["."]
extend-exclude = [
    "venv",
    ".venv",
    "node_modules",
    "fpl_output",
    "csv",
    "test",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
